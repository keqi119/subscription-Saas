# Web runtime
NODE_ENV=production
WEB_PORT=3000

# Browser-visible API base URL. This value is used at build time by Next.js.
NEXT_PUBLIC_API_BASE_URL=https://api.subauto.keybox.cloud/api

# Optional smoke target when the web app is running
SMOKE_WEB_BASE_URL=https://admin.subauto.keybox.cloud
