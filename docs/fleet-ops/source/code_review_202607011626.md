\*\*审查结论 / Review Conclusion\*\*

当前工作树是干净的，且本轮未改代码、未改数据。对照方案A，现有系统已经有较强的业务底座，尤其是 `Order / Lease / Vehicle / Finance / Report / ServiceCase`；但 PR-1 至 PR-10 在当前代码中还没有形成独立的 Fleet SaaS OS 引擎层。多数能力仍散落在既有服务中，下一步应优先做只读引擎抽象、证据链、置信度、Facade 与诊断能力。



The current tree is clean and no files/data were changed. Compared with Plan A, the system has a solid operational base, but PR-1 to PR-10 are not yet visible as independent Fleet OS engine modules. Most capabilities are embedded in existing services and should be lifted into read-only engines first.



\*\*能力对照 / Capability Map\*\*



| PR | 当前代码能力 / Current Capability | 状态 / Status | 后续方向 / Next Direction |

|---|---|---|---|

| PR-1 Vehicle Operational State | `Vehicle`、`Lease`、`SubscriptionOrder`、`ServiceCase`、`VehicleConditionReport` 信号存在，但没有独立 state resolver / confidence / evidence 层 | 部分具备 / Partial | 先实现只读状态引擎，严格分离 `types / rules / resolver / confidence / repository / service` |

| PR-2 Timeline / Digital Twin | Report detail 已拼装订单周期、账单、生命周期节点；审计、交付、还车、工单数据可用 | 部分具备 / Partial | 建立统一只读 timeline aggregator，按时间排序、去重、保留来源证据 |

| PR-3 Fleet KPI / Economic Intelligence | ReportService 已计算资产收益、利用率、ROA/ROE trial、折旧、残值、BaaS、资金占用 | 较强但未模块化 / Strong but monolithic | 从 ReportService 抽出 KPI/Economic Engine，并接入 PR-1/PR-2 的状态与停运数据 |

| PR-4 Risk / Collection | Finance 已有 overdue refresh、collection case/action、D1-D5 分层；Risk 主要是押金规则 | 部分具备 / Partial | 增加只读 risk scoring、collection strategy、arrears pipeline，不改变现有催收写流程 |

| PR-5 Guarded Execution | 多个服务内部已有 guard、audit、dryRun，但无统一 action guard 层 | 部分具备 / Partial | 先做只读 GuardedActionPlan evaluator，只产生命令建议，不执行写入 |

| PR-6 Advisory Optimization | 残值、收益、催收、工单数据可支持建议，但暂无 advisory engine | 缺失 / Missing | 建议引擎依赖 PR-1 至 PR-4 输出，提供可解释建议与置信度 |

| PR-7 Governance / Policy Evolution | 有签约政策、押金规则、折旧政策，但无统一 Fleet policy registry | 部分具备 / Partial | 建立政策目录、版本、适用范围与变更审计设计 |

| PR-8 Multi-Agent Coordination | 未发现独立 agent coordination 模块 | 缺失 / Missing | 延后到 PR-1 至 PR-7 稳定后，只做编排协议和只读 agent context |

| PR-9 Integration / Facade / Health / Invariants | 只有通用 `/health`，无 Fleet facade、无 invariant checks | 部分具备 / Partial | 建立 Fleet read facade 与 invariant diagnostics |

| PR-10 Release Readiness / Observability | 有 `release-check`、`api-smoke`、基础 health，但无 Fleet 专项诊断 | 部分具备 / Partial | 增加 Fleet 专项测试、smoke、readiness、diagnostic report |



\*\*关键代码依据 / Code Evidence\*\*



\- 模块注册仍是传统业务模块，未见独立 Fleet OS 引擎注册：\[app.module.ts](D:/Projects/auto-subscription-platform/apps/api/src/app.module.ts:1)

\- `Vehicle.status` 已有基础资产状态，但不是可解释运营状态：\[schema.prisma](D:/Projects/auto-subscription-platform/prisma/schema.prisma:2456)

\- 订单评审、交付、还车状态迁移主要集中在 OrderService：\[order.service.ts](D:/Projects/auto-subscription-platform/apps/api/src/order/order.service.ts:826)

\- Lease activation 是执行型引擎，不是 Fleet Ops 只读状态引擎：\[lease-activation.engine.ts](D:/Projects/auto-subscription-platform/apps/api/src/lease/lease-activation.engine.ts:26)

\- 催收能力在 FinanceService 中较完整，但仍偏执行流程：\[finance.service.ts](D:/Projects/auto-subscription-platform/apps/api/src/finance/finance.service.ts:727)

\- 资产收益/KPI 主要在 ReportService 中，能力强但耦合较高：\[report.service.ts](D:/Projects/auto-subscription-platform/apps/api/src/report/report.service.ts:597)



\*\*方案A差距 / Plan A Gaps\*\*



\- Fleet Operations Engine 尚未独立成层。  

&#x20; Fleet Operations is not yet an independent layer.



\- 当前 `Vehicle.status` 容易与 Order、Lease、ServiceCase、ConditionReport 产生冲突，缺少优先级、证据链和置信度。  

&#x20; `Vehicle.status` can conflict with order, lease, service case, and condition report signals without priority, evidence, or confidence handling.



\- Utilization 当前更偏租赁天数口径，未纳入 downtime、maintenance、service-case impact。  

&#x20; Utilization is lease-day based and not downtime-aware.



\- Collection read model 依赖 refresh 后的账单状态，若 refresh 未运行，存在 overdue underreporting 风险。  

&#x20; Collection read models may underreport overdue exposure if refresh has not run.



\- Financial Intelligence 已有大量能力，但集中在 ReportService，后续复用和治理成本较高。  

&#x20; Financial intelligence exists but is concentrated inside ReportService.



\*\*后续开发计划 / Development Plan\*\*



1\. \*\*P0：PR-1 Vehicle Operational State Engine\*\*

&#x20;  建立只读模块，严格拆分 `types / rules / resolver / confidence / repository / service`。输出 state、priority reason、evidence、confidence、conflict list。  

&#x20;  Build the read-only state engine first with deterministic resolution, evidence tracking, and confidence scoring.



2\. \*\*P0：PR-2 Timeline / Digital Twin Engine\*\*

&#x20;  聚合订单、租约、交付、还车、工单、车况报告、账单、审计事件，形成车辆级时间线。  

&#x20;  Aggregate vehicle-level lifecycle events into a canonical timeline.



3\. \*\*P0：PR-9 Fleet Facade + Invariants\*\*

&#x20;  增加只读 Facade，统一暴露 operational state、timeline、KPI、risk summary，并增加 invariant diagnostics。  

&#x20;  Add a read-only facade and invariant checks for cross-module consistency.



4\. \*\*P1：PR-3 KPI / Economic Engine 拆分\*\*

&#x20;  从 ReportService 抽出 Fleet KPI/Economic Engine，保留现有报表输出兼容。加入 downtime-aware utilization、downtime rate、ROI per vehicle、fleet IRR、cashflow model。  

&#x20;  Extract reusable KPI/economic logic while keeping existing report behavior stable.



5\. \*\*P1：PR-4 Collection Intelligence\*\*

&#x20;  在现有 collection case 基础上增加只读 risk score、strategy recommendation、arrears pipeline。  

&#x20;  Add risk scoring and strategy recommendations without changing the current write flow.



6\. \*\*P1/P2：PR-5 Guarded Execution\*\*

&#x20;  先实现 action proposal 和 guard evaluation，只输出建议与阻断原因，不执行自动化写入。  

&#x20;  Start with action planning and guard evaluation only.



7\. \*\*P2：PR-6 / PR-7\*\*

&#x20;  Advisory 与 Governance 依赖前面引擎稳定后再做。重点是建议可解释、政策可追踪、版本可审计。  

&#x20;  Advisory and governance should follow once core state, timeline, KPI, and risk engines are stable.



8\. \*\*P3：PR-8 Multi-Agent Coordination\*\*

&#x20;  最后再做 agent coordination，避免在底层状态和政策未稳定时引入复杂编排。  

&#x20;  Defer multi-agent orchestration until the underlying engines are stable.



9\. \*\*持续：PR-10 Release Readiness\*\*

&#x20;  每个 PR 增加专项单测、只读回归、smoke endpoint、diagnostic checklist，不做生产部署自动化。  

&#x20;  Add PR-specific tests, smoke checks, and diagnostics continuously.



\*\*优先级建议 / Priority\*\*

先做 `PR-1 -> PR-2 -> PR-9 -> PR-3 -> PR-4`。这条路径能先解决状态可信、时间线可追溯、跨模块可诊断的问题，再把财务和催收智能建立在稳定底座上。

