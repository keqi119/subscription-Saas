# 纯电汽车订阅运营中台开发文档包

包含：

- `DEV_SPEC.md`：完整开发文档
- `AGENTS.md`：建议放到仓库根目录，给 Codex 使用
- `CODEX_TASKS.md`：分阶段开发提示词

建议用法：

1. 将 `DEV_SPEC.md` 和 `AGENTS.md` 放入 GitHub 仓库根目录。
2. 每次只复制 `CODEX_TASKS.md` 中的一个任务给 Codex。
3. 完成一个任务后检查运行结果、数据库迁移和测试。
4. 不要一次性让 Codex 实现全部模块。
